# ./scripts/database

This entire directory only contains historic data and will not be updated in the future.

Unfortunately, early on in the project proper protocals for migrations were not followed.
Meaning we manually added updates and logs on why changes were made to these files here.

But, now that we do properly use migrations, please consult `./scripts/migrations` for
the actual real status of the production database, use this directory for historic purposes only.
